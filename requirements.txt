fastapi
uvicorn[standard]
yt-dlp>=2026.3.17
pandas
google-auth
google-auth-oauthlib
google-api-python-client

# --- Agente RAG ---
rank_bm25
sentence-transformers  # CrossEncoder para re-rankeamento semântico pós-BM25
openai
anthropic
google-generativeai  # SDK usado pelo chat Gemini (import google.generativeai)

# --- Processamento de documentos ---
pdfplumber
python-docx
python-multipart
reportlab  # geração de PDF no export /export/relatorio-pdf

# --- Imagens e áudio (opcional — instalação separada) ---
# pytesseract   # OCR fallback para imagens (requer binário Tesseract no sistema)
# faster-whisper  # transcrição de áudio local (CPU, ~150 MB modelo base)
