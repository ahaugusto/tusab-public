fastapi
uvicorn[standard]
yt-dlp
pandas
google-auth
google-auth-oauthlib
google-api-python-client

# --- Agente RAG ---
rank_bm25
openai
anthropic
google-generativeai  # SDK usado pelo chat Gemini (import google.generativeai)

# --- Processamento de documentos ---
pdfplumber
python-docx
python-multipart

# --- Imagens e áudio (opcional — instalação separada) ---
# pytesseract   # OCR fallback para imagens (requer binário Tesseract no sistema)
# faster-whisper  # transcrição de áudio local (CPU, ~150 MB modelo base)
